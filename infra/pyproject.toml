[project]
name = "sage-infra"
version = "0.0.0"
description = "AWS CDK app (Python) — data / api / agents / frontend stacks"
requires-python = ">=3.12"
dependencies = [
  "aws-cdk-lib>=2.150",
  "constructs>=10.3",
]

[dependency-groups]
dev = ["pytest>=8"]
