# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/

# uv (lockfile IS committed for reproducible installs)

# Node / Next.js
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
pnpm-debug.log*

# Env / secrets
.env
.env.*
!.env.example
!web/.env.local.example

# Data artifacts (synthetic dataset, snapshots, parquet) — NOT the top-level
# /data package (generator/warehouse/detectors source, tracked); runtime output
# lives under /var/data instead to avoid colliding with that directory name.
/var/data/
*.parquet
*.duckdb
*.sqlite

# AWS CDK
platform/infra/cdk.out/
platform/infra/.cdk.staging/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp
