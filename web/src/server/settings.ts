// Server-only env settings: OpenClaw base URL/token, agent ids, database URL,
// and anything else this app's API routes need beyond NEXT_PUBLIC_*. See
// .env.example. Never import from a client component (use NEXT_PUBLIC_* env
// vars there instead).
// STUB — structure only, no implementation yet.

export {};
