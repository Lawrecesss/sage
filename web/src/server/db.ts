// DB access for the API routes — Postgres client/session, demo-user (no auth
// for the demo). Server-only — never import from a client component.
// STUB — structure only, no implementation yet.

export {};
