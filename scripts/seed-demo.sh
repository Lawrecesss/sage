#!/usr/bin/env bash
# Generate the synthetic dataset, load the warehouse, run detectors.
set -euo pipefail
echo "STUB: sage-generate --months 12 --seed 42 && sage-warehouse load && sage-detectors run"
