"""sage_simulator — synthetic SME dataset + planted incident library.

Entity shapes (Supplier, Sku, CustomerSegment) live in `sage_models`; this
package only seeds instances of them with realistic signal and noise.
"""
