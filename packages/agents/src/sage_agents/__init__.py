"""sage_agents — Strands agents. Detection is deterministic; the agents interpret.

The LLM is reached through the hackathon organisers' Ollama-compatible endpoint
(Bedrock-backed). One model for every agent — Claude Sonnet 4.5. See
docs/decisions/0002-lightsail-single-instance.md.
"""
