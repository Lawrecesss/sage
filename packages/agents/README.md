# packages/agents

Strands agents, tool surface, prompts, Bedrock integration. Owned by M2. Watcher (per domain) · Analyst/Correlator (the product) · Briefing · Ask. Every tool is a client-side Python tool. Cache the metric catalog + system prompt behind a cache_control breakpoint.

> STUB — structure only, no implementation yet.
