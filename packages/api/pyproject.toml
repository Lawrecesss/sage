[project]
name = "sage-api"
version = "0.0.0"
description = "FastAPI service (Mangum on Lambda + API Gateway)"
requires-python = ">=3.12"
dependencies = [
  "sage-shared",
  "sage-warehouse",
  "sage-agents",
  "fastapi>=0.115",
  "mangum>=0.19",
  "uvicorn[standard]>=0.30",
  "sse-starlette>=2.1",
  "boto3>=1.35",
  "pydantic>=2.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sage_api"]
