# packages/detectors

Deterministic anomaly detection — no LLM. Owned by M1. z-score / WoW / threshold over the metric layer, writes the signals table with a dollar-impact estimate, ranks so a brief has <=5 items.

> STUB — structure only, no implementation yet.
